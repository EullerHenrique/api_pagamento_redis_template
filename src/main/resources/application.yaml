spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/db
    username: euller
    password: 12345
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
    password: 12345

